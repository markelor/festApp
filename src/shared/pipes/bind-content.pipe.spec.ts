import { BindContentPipe } from './bind-content.pipe';

describe('BindContentPipe', () => {
  it('create an instance', () => {
    const pipe = new BindContentPipe();
    expect(pipe).toBeTruthy();
  });
});
